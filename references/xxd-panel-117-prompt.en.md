# Panel 117 runtime adapter

Read the complete `original-prompt/zh-CN.md` before every generation; append these delivery settings without replacing or translating its aesthetic requirements.

- `top-bottom`: keep the original 3:4 top/bottom presentation unless another size is explicitly selected. Photograph above, design below, exactly 50:50.
- `left-right`: explicitly replace all original above/below delivery references with left/right. Photograph left, design right, each half of the width and full canvas height. No stacked regions or third band. The fine-line pale-colour aesthetic remains unchanged.
- `design-only` / `wallpaper-pack`: use the entire canvas for the designed region; the photograph is an invisible reference. Wallpaper devices use their resolved sizes and `linked` or `independent` relationship.
- Text: `prompt`, `exact` or `none`; resolve locale explicitly. Prompt copy follows each source and fine handwritten typography. Samples use English; general tasks use the user's resolved locale.
- Sizes: `auto`, `source`, common or custom ratios, or exact pixels. Explicit size overrides only original canvas dimensions.
- One image or recursively inventoried directory batch; one independent generation from each original per output. Never feed samples or intermediate stylisations back into generation.
